# 보안 및 개인정보 취급 원칙

본 리포지토리는 국고보조사업 정산 원장을 보관한다. 정산 데이터의 성격상 개인정보와
사업자 정보가 포함되므로, 아래 원칙을 **예외 없이** 적용한다.

---

## 1. 리포지토리는 private으로만 운영한다

`data/evidence.json`에는 다음이 포함된다.

- 참여인력 **성명 + 생년월일** (인건비·여비 세부집행내역)
- 입찰 평가위원 **성명 + 생년월일** (평가위원 수당)
- 거래처 **상호 + 사업자등록번호**
- 계약금액·집행금액 등 계약 상대방의 영업정보

공개 전환은 어떤 사유로도 허용하지 않는다. Fork·Transfer 역시 금지한다.

## 2. 생년월일은 마스킹하여 저장한다

일(日) 자리를 `**`로 치환한 형식만 허용한다.

```
허용    "생년월일": "'82.05.**"
금지    "생년월일": "'82.05.10"
```

검증규칙 **R-21**이 이를 강제하며, 위반 시 CI가 실패한다.

제출용 원본(마스킹 해제본)이 필요한 경우 GitHub이 아닌 별도 보안 저장소에서
생성하며, 생성기에 해제 값을 주입하는 방식으로만 처리한다.
해제본 파일은 `.gitignore`에 의해 커밋이 차단된다(`*.raw.json`, `*.unmasked.*`).

## 3. GitHub Pages에는 집계값만 배포한다

대시보드가 공개 URL로 노출되므로 다음만 게시한다.

| 게시 가능 | 게시 금지 |
|---|---|
| 비목별 예산·집행액·집행률 | 세부집행내역(성명·생년월일) |
| 증빙 확보율, 결손 건수 | 거래처 사업자등록번호 |
| 준공 진척률, 기한 D-day | 계좌번호, 계약 단가 |

## 4. 커밋해서는 안 되는 것

- 증빙 원본 파일(세금계산서·통장 사본·급여대장 등) — `파일링크`에 URL만 보관
- 계좌번호 전체 — `baseline.json`의 `계좌번호_마스킹` 필드만 사용
- API 키, 토큰, 비밀번호 — GitHub Secrets 사용
- 마스킹 해제된 개인정보

## 5. 접근 권한

| 역할 | 권한 | 대상 |
|---|---|---|
| Admin | 전체 | PMO 총괄(상무 이성호) |
| Write | 원장 수정 PR | PMO 정산 담당 |
| Read | 조회 | 컨소시엄 기관 담당자, 검증기관 |

`main` 브랜치는 보호 설정을 적용하고, 직접 push를 금지하며 PR + CI 통과를 필수로 한다.

## 6. 자료 보관

통합관리지침 §31에 따라 5년 보관이 법정 의무이나, 본 사업은 감사 대응을 위해
**2031년까지** 리포지토리를 유지한다. 사업 종결 후에도 삭제·아카이브 해제를 하지 않는다.

## 7. 사고 발생 시

개인정보가 포함된 커밋이 이루어진 경우, 커밋 되돌리기(revert)만으로는 이력에서
제거되지 않는다. 즉시 PMO 총괄에게 보고하고 다음을 수행한다.

1. 리포지토리 접근 차단
2. `git filter-repo` 등으로 이력 재작성 후 force push
3. 노출 기간·범위 기록 및 개인정보 보호법상 통지 의무 검토
