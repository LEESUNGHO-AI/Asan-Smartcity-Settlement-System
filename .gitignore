# 의존성
node_modules/

# 생성 산출물 — 공개 저장소에서는 대시보드에서 바로 받을 수 있도록 커밋한다
# (비공개 전환 시 아래 주석을 해제하십시오)
# output/
validation-report.json

# 마스킹 해제본(제출용 원본) — 절대 커밋 금지
*.raw.json
data/**/*.unmasked.*

# 환경/임시
.env
.DS_Store
*.log
~$*
sync-warnings.txt
sync.log
