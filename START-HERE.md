# 처음부터 구축하기

터미널 없이 **GitHub 웹 화면 · Notion · Slack 화면**만으로 끝낼 수 있게 정리했다.
전부 합쳐 약 2시간. 한 번에 다 하지 않아도 되며, 단계마다 끊어도 된다.

```
1단계  GitHub 리포 만들기          20분   ← 여기까지만 해도 검증기가 돈다
2단계  Notion 준비                20분
3단계  Notion DB 자동 생성         10분
4단계  Slack 채널·양식 만들기       30분
5단계  Slack↔Notion 연결(GAS)      20분
6단계  전체 흐름 테스트            20분
```

각 단계 끝에 **[확인]** 이 있다. 그게 보이면 다음으로 넘어간다.

---

# 1단계 · GitHub 리포 만들기

## 1-1. 리포지토리 생성

github.com → 우측 상단 `+` → **New repository**

| 항목 | 값 |
|---|---|
| Owner | `LEESUNGHO-AI` |
| Repository name | `Asan-Smartcity-Settlement-System` |
| **Visibility** | **Private** ⚠️ |
| Add a README file | **체크 해제** |
| Add .gitignore | None |
| Choose a license | None |

`Create repository` 클릭.

> ⚠️ **Private을 반드시 확인한다.** 참여인력·평가위원의 성명과 생년월일이 들어간다.
> Public으로 만들면 개인정보 유출이고, 나중에 Private으로 바꿔도 이미 노출된 것은 되돌릴 수 없다.

## 1-2. 파일 올리기

만들어진 빈 리포지토리 화면에서 **uploading an existing file** 링크 클릭.

받으신 `Asan-Smartcity-Settlement-System` 폴더를 열고,
**폴더 자체가 아니라 안에 있는 것들을 전부 선택**해서 드래그한다.

- `.github`, `codes`, `data`, `docs`, `integrations`, `rules`, `schema`, `scripts` 폴더
- `README.md`, `SECURITY.md`, `SETUP.md`, `CHANGELOG.md`, `package.json`, `package-lock.json`
- `.gitignore`, `.editorconfig`, `.gitattributes`

> `node_modules` 폴더와 `.git` 폴더는 올리지 않는다. `output` 폴더도 올리지 않는다.
> 숨김 파일(`.`으로 시작)이 안 보이면, 파인더는 `⌘⇧.`, 탐색기는 보기 → 숨긴 항목 체크.

아래 `Commit changes` 칸에 이렇게 적고 커밋한다.

```
feat: 아산시 강소형 스마트시티 정산시스템 초기 구축
```

## 1-3. 브랜치 보호

Settings → 좌측 **Branches** → `Add branch protection rule`

| 항목 | 값 |
|---|---|
| Branch name pattern | `main` |
| Require a pull request before merging | ✔ |
| Require status checks to pass before merging | ✔ |

원장을 아무나 직접 못 고치게 막는 것이 이 시스템의 핵심이다.

**[확인]** Actions 탭에 `원장 검증`이 실행되고 **빨간 X**가 뜬다.
이건 정상이다. 클릭해 들어가면 이렇게 나온다.

```
✗ R-01  기준정보 미확정 — 협약서·교부결정통지서 확인 필요
✗ R-03  계약서 등록 지연 20일
✗ R-06  무형자산 3건(38.25억) 중요재산 미등재
✗ R-11  낙찰차액 80,000,000원
```

시스템이 지금 정산 상태의 결함을 정확히 짚고 있는 것이다.

---

# 2단계 · Notion 준비

## 2-1. 통합(Integration) 만들기

브라우저에서 `notion.so/my-integrations` 접속 → **New integration**

| 항목 | 값 |
|---|---|
| Name | `아산 정산시스템` |
| Associated workspace | 본인 워크스페이스 |
| Type | Internal |

만든 뒤 **Capabilities** 탭에서 아래를 켠다.

- ✔ Read content
- ✔ Update content
- ✔ Insert content
- ✔ No user information (개인정보 최소 수집)

**Configuration** 탭 → `Internal Integration Secret` → **Show** → 복사.
`ntn_` 또는 `secret_`으로 시작하는 긴 문자열이다. **메모장에 붙여둔다.**

## 2-2. DB를 담을 페이지 만들기

Notion에서 새 페이지를 만들고 이름을 `아산 강소형 정산시스템`이라고 한다.

그 페이지 우측 상단 `···` → **연결** → 방금 만든 `아산 정산시스템` 선택.

## 2-3. 페이지 ID 알아내기

같은 `···` → **링크 복사**. 이런 주소가 나온다.

```
https://www.notion.so/아산-강소형-정산시스템-2b850aa9577d8128ad35d86b79f67d12?pvs=4
                                              └────────── 이 32자리 ──────────┘
```

`?` 앞의 32자리 문자열이 페이지 ID다. **메모장에 붙여둔다.**

**[확인]** 메모장에 두 개가 있다 — 통합 토큰, 페이지 ID.

---

# 3단계 · Notion DB 자동 생성

속성을 30개나 손으로 만들면 반드시 오타가 난다. 오타 난 필드는 조용히 비어서
정산에 반영되므로, 스크립트로 만든다. **터미널은 필요 없다.**

## 3-1. GitHub Secrets 등록

리포지토리 → Settings → 좌측 **Secrets and variables** → **Actions**
→ `New repository secret`

| Name | Secret |
|---|---|
| `NOTION_TOKEN` | 2-1에서 복사한 토큰 |
| `NOTION_PARENT_PAGE` | 2-3에서 복사한 페이지 ID |

## 3-2. 미리보기 먼저

Actions 탭 → 좌측 **Notion DB 생성** → 우측 `Run workflow`

| 항목 | 값 |
|---|---|
| 작업 방식 | 미리보기 (아무것도 만들지 않음) |
| 대상 DB | 전체 |

초록 체크가 뜨면 클릭 → **Summary** 화면에 만들어질 속성 목록이 보인다.
정산증빙 30개 / 계약대장 25개 / 중요재산 25개 / 준공산출물 22개.

## 3-3. 실제 생성

같은 워크플로를 **작업 방식: 새로 생성** 으로 다시 실행한다.

끝나면 Summary 하단에 이런 게 나온다.

```
 GitHub Secrets 에 등록할 값
  NOTION_EVIDENCE_DB   = 2c150aa9577d81...
  NOTION_CONTRACTS_DB  = 2c150aa9577d82...
  NOTION_ASSETS_DB     = 2c150aa9577d83...
  NOTION_COMPLETION_DB = 2c150aa9577d84...
```

이 네 개를 **3-1과 같은 방법으로 Secrets에 추가로 등록**한다.

## 3-4. Notion 뷰 만들기

Notion API로는 뷰를 만들 수 없어서 이건 손으로 한다.
`정산증빙 DB`를 열고 상단 탭 옆 `+`를 눌러 5개를 만든다.

| 뷰 이름 | 방식 | 설정 |
|---|---|---|
| 보완필요 | 표 | 필터: 검토상태 = 보완필요 |
| 미확정 | 표 | 필터: 검토상태 ≠ 확정 |
| 이번달 | 표 | 필터: 집행일자 = 이번 달 |
| 기관별 | 보드 | 그룹: 기관 |
| 비목별 | 표 | 그룹: 보조비목, 집행금액 합계 표시 |

**[확인]** Notion에 DB 4개가 생겼고, 정산증빙 DB의 드롭다운을 눌러보면
기관 5개·단위사업 9개·보조비목 12개가 이미 채워져 있다.

---

# 4단계 · Slack 채널·양식 만들기

## 4-1. 채널 만들기

| 채널 | 설명 |
|---|---|
| `#정산-증빙` | 증빙 등록 |
| `#정산-알림` | 검증 결과 자동 알림 (봇 전용) |
| `#준공서류` | 준공산출물 |

`#정산-알림`은 사람이 글을 못 쓰게 해둔다. 사람이 끼어들면 알림이 묻힌다.

## 4-2. 알림용 Webhook 만들기

`api.slack.com/apps` → **Create New App** → From scratch
→ 이름 `아산 정산봇`, 워크스페이스 선택

좌측 **Incoming Webhooks** → 켜기 → `Add New Webhook to Workspace`
→ 채널 `#정산-알림` 선택 → 허용

생성된 URL(`https://hooks.slack.com/services/...`)을 복사해
GitHub Secrets에 `SLACK_WEBHOOK_URL` 로 등록한다.

## 4-3. 봇 토큰 만들기

같은 앱에서 좌측 **OAuth & Permissions** → Bot Token Scopes에 추가

- `chat:write`
- `channels:read`

상단 `Install to Workspace` → 허용 → `Bot User OAuth Token` (`xoxb-`로 시작) 복사.
**메모장에 붙여둔다.** (5단계에서 쓴다)

그리고 Slack에서 `#정산-증빙` 채널에 들어가 `/invite @아산 정산봇` 을 친다.

## 4-4. 증빙 등록 양식 만들기

Slack 좌측 하단 `···` 더보기 → **자동화** → **워크플로** → `새 워크플로`

**시작 방식**: `링크에서 시작하기` 선택

**단계 추가 → 양식 보내기**. 아래 질문을 순서대로 넣는다.
자세한 선택지 목록은 리포지토리의 `integrations/slack/SETUP.md`에 있으니
복사해서 붙여넣으면 된다.

| 질문 | 유형 |
|---|---|
| 기관 | 드롭다운 (ASAN, JEIL, HOSEO, CNI, KAIST) |
| 단위사업 | 드롭다운 (SP-OASIS 등 9개) |
| 보조비목 | 드롭다운 (인건비, 운영비, 여비 등) |
| 보조세목 | 드롭다운 (보수, 일반수용비, 시설비 등) |
| 재원 | 드롭다운 (국비, 도비, 시비, 자기부담금) |
| 지급처구분 | 드롭다운 (사업자, 개인) |
| 지급처명 | 단답 |
| 사업자등록번호 | 단답 (선택) |
| 생년월일 | 단답 (선택) |
| 참여율 | 단답 (선택) |
| 집행일자 | 날짜 |
| 집행금액 | 단답 |
| 사용목적 | 장문 |
| 지급방식 | 드롭다운 (계좌이체, 보조금전용카드 등) |
| 증빙유형 | 다중선택 |
| 파일링크 | 단답 (선택) |

> **왜 이렇게 번거롭게 하나?**
> "11월 회의비 8만원 카드" 같은 자유 텍스트에서 비목·세목·재원을 짐작하면 반드시 틀린다.
> 정산에서 한 건의 오분류는 총괄명세서 전체를 어긋나게 한다. 선택지를 고르게 하는 것이
> 예산 외 비목 집행을 입력 단계에서 막는 방법이다.

**[확인]** 워크플로를 저장하고 링크를 `#정산-증빙` 채널에 고정해둔다.
아직 Notion으로 넘어가지는 않는다. 5단계에서 연결한다.

---

# 5단계 · Slack ↔ Notion 연결 (Google Apps Script)

## 5-1. 스크립트 만들기

`script.google.com` 접속 (계정: `airlan506@gmail.com`) → **새 프로젝트**

프로젝트 이름을 `아산 정산 Slack연동` 으로 바꾼다.

리포지토리의 `integrations/slack/gas/Code.gs` 파일 내용을 **전부 복사**해서
편집기의 기존 내용을 지우고 붙여넣는다. 저장(💾).

## 5-2. 비밀값 등록

좌측 톱니바퀴 **프로젝트 설정** → 하단 **스크립트 속성** → `속성 추가`

| 속성 | 값 |
|---|---|
| `NOTION_TOKEN` | 2-1의 토큰 |
| `NOTION_EVIDENCE_DB` | 3-3의 정산증빙 DB ID |
| `SLACK_BOT_TOKEN` | 4-3의 `xoxb-...` |

## 5-3. 연결 확인

편집기 상단 함수 선택 칸에서 `testConnection` 을 고르고 **실행**.
처음엔 권한 승인 창이 뜬다 — 허용한다.

하단 실행 로그에 이렇게 나오면 성공이다.

```
DB: 정산증빙 DB
속성 30개
✅ 필수 속성 모두 존재
```

`❌ 누락 속성` 이 나오면 3-3이 제대로 안 된 것이다. 3단계를 다시 확인한다.

## 5-4. 웹 앱으로 배포

우측 상단 **배포** → **새 배포** → 톱니바퀴 → **웹 앱**

| 항목 | 값 |
|---|---|
| 실행 계정 | 나 |
| 액세스 권한 | **모든 사용자** |

배포 후 나오는 **웹 앱 URL**을 복사한다.

## 5-5. Slack 워크플로에 연결

4-4에서 만든 워크플로를 다시 열고 → **단계 추가** → **웹 요청 보내기**

- URL: 5-4의 웹 앱 URL
- 본문: `integrations/slack/SETUP.md`의 JSON 블록을 복사해 붙여넣고,
  `{{ }}` 안을 각 폼 변수로 연결한다. `channel_id`와 `team_domain`은 직접 채운다.

마지막에 **단계 추가 → 메시지 보내기**로 `#정산-증빙`에 요약을 남긴다.
GAS가 이 스레드에 결과를 회신한다.

**[확인]** 워크플로 게시.

---

# 6단계 · 전체 흐름 테스트

## 6-1. 일부러 틀려보기

`#정산-증빙`에서 워크플로를 실행해 아래처럼 넣는다.

- 지급방식: **개인카드**
- 사용목적: **회의**

스레드에 이런 회신이 와야 한다.

```
❌ 등록 실패
• ⚠ 개인카드은(는) 보조금 인정 지급방식이 아닙니다(지침 §18①). 정산 불인정 위험
• 사용목적이 너무 짧습니다 — 사업 관련성이 드러나게 기재하십시오
```

**이게 뜨면 시스템이 제대로 작동하는 것이다.** 잘못된 데이터가 Notion에
들어가기 전에 막힌 것이다.

## 6-2. 개인정보 마스킹 확인

지급처구분을 **개인**으로 하고 생년월일에 `'82.05.10` 을 넣어본다.

Notion에서 해당 레코드를 열어 생년월일이 **`'82.05.**`** 로 저장되어 있으면 정상이다.
원본이 그대로 들어갔다면 5-1의 코드 붙여넣기가 잘못된 것이다.

## 6-3. 제대로 한 건 넣기

- 기관 `JEIL` / 단위사업 `SP-NET` / 보조비목 `건설비` / 보조세목 `시설비`
- 재원 `국비` / 지급처구분 `사업자` / 지급처명 `주식회사 싸인텔레콤`
- 집행일자 오늘 / 집행금액 `1000000`
- 사용목적 `유무선 네트워크 구축 용역 테스트 건`
- 지급방식 `계좌이체` / 증빙유형 `전자세금계산서`, `계좌이체증`

스레드에 `✅ 정산증빙 등록 완료` 가 오고 Notion에 레코드가 생긴다.

## 6-4. GitHub으로 가져오기

Actions → **일일 동기화** → `Run workflow` (변경사항만 확인: 체크 해제)

끝나면 `data/evidence.json`에 방금 넣은 건이 들어가고,
Notion의 증빙ID 칸에 `EV-2026-000XX` 가 자동으로 채워진다.

`#정산-알림` 채널에 검증 결과가 온다.

```
🚨 정산 원장 검증 실패    오류 6건 | 경고 16건
해소해야 할 항목
• R-06  3건 — 중요재산 미등재·보고 지연 (법 §35)
• R-01  1건 — 기준정보 미확정
```

## 6-5. 보고서 뽑아보기

Actions → **집행결과 제출 양식 생성** → `Run workflow`
(기관 `JEIL`, 기간 `2025-10-01` ~ `2026-06-30`)

> 검증 오류가 있으면 생성이 중단된다. 지금은 R-01 때문에 막힌다.
> 시험 삼아 보시려면 로컬에서 `npm run report` 로 돌리면 초안 워터마크가 찍힌 채 나온다.

**[확인]** 여기까지 되면 Slack → Notion → GitHub → Slack 한 바퀴가 완성된 것이다.

---

# 이후 해야 할 일

시스템은 다 만들어졌다. 남은 것은 **데이터와 기준**이다.

| 순서 | 할 일 | 왜 |
|---|---|---|
| 1 | 질의서 2건 발송 (`docs/inquiry/`) | 회신에 시간이 걸린다. 제일 먼저 보낸다 |
| 2 | 협약서·교부결정통지서 확보 → `data/baseline.json` 확정 | R-01이 풀려야 보고서가 나온다 |
| 3 | 무형자산 3건 중요재산 등록 | 15일 보고 기한이 이미 지났다 |
| 4 | 증빙 약 700건 이관 | 원본 엑셀이 있으면 일괄 변환 가능 |
| 5 | 낙찰차액 처리방침 결정 | 변경승인은 10월 전에 해야 한다 |

---

# 막혔을 때

| 증상 | 원인 | 조치 |
|---|---|---|
| Actions가 계속 빨간 X | R-01 등 실제 결함 | 정상이다. 위 "이후 해야 할 일" 2번 |
| `Notion 404` | 통합을 페이지에 연결 안 함 | 2-2 다시 |
| `Notion 401` | 토큰 오타 | Secrets 재등록 |
| GAS `❌ 누락 속성` | DB가 덜 만들어짐 | Actions → Notion DB 생성 → **기존 DB 속성 보강** |
| Slack 응답 없음 | 웹 앱 액세스가 "나만" | 5-4에서 **모든 사용자**로 재배포 |
| 스레드 회신 없음 | 봇 미초대 | `#정산-증빙`에서 `/invite @아산 정산봇` |
| 동기화 시 "보조세목 없음" | 비목·세목 조합 오류 | 인건비→보수 처럼 짝이 맞아야 한다 |

---

*문의: ㈜제일엔지니어링종합건축사사무소 PMO · 상무 이성호*
